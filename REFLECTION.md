# SafeFix Harness 项目反思

## 一、从“让模型写代码”到“约束模型完成工程”

完成 SafeFix Harness 后，我最直接的认识是：模型生成代码并不是最困难的部分，真正困难的是定义边界、提供反馈并判断结果是否满足需求。本项目的大部分实现由受约束的子代理完成，我的重点是确定方向、接受或否决建议、收缩范围，并根据测试和评审证据决定能否合并。人的价值由此转移到了目标选择、风险判断和最终责任上。

## 二、Superpowers 技能的实际作用

帮助最大的技能是 brainstorming、TDD、subagent-driven-development、代码评审和完成前验证。brainstorming 的价值不是生成长文档，而是把宽泛的 Coding Agent Harness 收敛成面向小型缺陷修复的 SafeFix，并确定治理护栏为主要贡献。工作区围栏、三级风险策略、人工审批和反馈回灌都由此展开，避免了功能很多但机制都很浅的问题。

TDD、独立评审和完成前验证构成了质量闭环。策略引擎通过基本测试后，评审仍发现程序名冒充、解释器内嵌命令、Git 外部执行和敏感文件搬运等绕过路径；审计模块也暴露了 SQLite trigger、重入写入和中断清理问题。解决方式都是先构造确定性失败，再修改实现。

部分流程在简单任务上则形式大于实质。每个小改动都建立 worktree、生成报告、两阶段评审并更新多份文档，管理成本很高。T09 至 T16 采用快速交付模式后，仍用聚焦测试和全量回归保留基本门槛，说明流程强度应随风险调整。入口类技能经常重复已知规则，新增价值有限。

## 三、TDD 是阻碍还是放大器

TDD 在局部会减慢编码，但总体是放大器。模型能迅速写出“看起来合理”的实现，却容易遗漏边界；没有先定义失败，就难以判断它是在修复还是仅仅改动代码。UTC 时间约束、异常局部变量泄密和 wheel 缺少运行时依赖，都是得到明确 RED 后才可靠修复。

它的局限是测试质量决定了能放大什么。T04 的属性测试曾复用生产比较思路，并把拒绝都视为通过；即使全绿，也可能重复实现的盲点。“先测试”不自动等于正确，测试判据和反例仍需审查；低风险机械调整强求完整 RED—GREEN 也会增加噪声。

## 四、子代理自主性与最佳任务粒度

子代理能否长时间不偏离，主要取决于任务边界。最稳定的任务通常限于一两个生产文件及其测试，并给出冻结基线、输入输出、禁止范围和验证命令。T05 只围绕策略引擎展开，经历多轮修复仍未偏题；后期“可用 CLI”同时涉及运行时、凭据、隔离、终端、分发和文档，修复轮次明显增加。

最优粒度应是“一个可独立验证的契约”：单次上下文能解释清楚，修改范围可列举，失败测试可单独运行，结果可独立评审。过细会让协调成本超过实现成本；过粗则会让代理自行补全假设。

## 五、SPEC 与 PLAN 如何影响实现

陌生智能体冷启动是本项目最有价值的一次规约检查。它仅凭最初的 SPEC 和 PLAN 执行 T01，在 Action 的 `type` 判别值未定义处暂停；同时暴露了 `PolicyDecision.risk` 与 `risk_level` 命名冲突、多个枚举值缺失、RunSnapshot 字段不完整，以及首次运行 pytest 前没有安装依赖等问题。若让它继续猜测，很可能会生成与后续任务不兼容的数据模型。这不是代理能力不足，而是规约把关键决定留在了共同对话的隐性上下文中。

修订后，七类 Action 的字段、默认值和约束被明确列出，反馈与进展判断拆分，Windows 路径算法和 GitWildMatch 语义也被固定。后续实现质量因此明显提高。这个案例说明，SPEC 决定“做什么”，PLAN 决定“怎样证明做完”；两者缺少任何一方，代码质量评审都只能在错误目标上优化。

## 六、有效的 Prompt 与 Context 策略

最有效的策略是最小充分上下文。实现代理只获得任务说明、工作树、基线、允许文件、RED—GREEN 要求和报告格式；评审代理获得同一说明与冻结差异并保持只读；修复代理只接收已确认问题和复现输入。这既减少上下文污染，也避免无关重构。

另一项策略是要求客观证据：记录失败数、退出码、测试范围和未执行项。Docker 未运行时只记录阻塞，不把配置存在当成镜像已验证；wheel smoke 曾继承 `PYTHONPATH`，复审时清除变量并确认实际加载 site-packages。可复现输入和明确边界比“请仔细检查”更可靠。

## 七、凭据与分发带来的工程认识

凭据要求让我认识到，秘密不只从配置和日志泄露，还可能出现在异常链、traceback locals、审计 metadata 和终端输出中。SafeFix 因此把非披露落实到异常边界和测试。分发则说明源码环境可用不代表 fresh wheel 能运行，CI 文件存在也不代表发布成功；隔离安装、入口检查和端到端旅程才是可用证据。

## 八、重做选择与方法论批判

如果重做，我会更早确定最小范围，只保留 AgentLoop、治理、反馈、审批、Mock 演示和 wheel 分发，不再先扩张到 Docker、Render 和对话 CLI 后再删除。我也会按风险设置门禁：安全边界和状态机采用完整 TDD 与双评审，普通展示和文档使用较轻流程。

Superpowers 假设需求可逐步冻结、任务可清晰拆分、测试能提供可靠判据，且额外代理成本可接受。这在策略、审批和反馈等确定性机制上大体成立，在交互体验、范围取舍和“是否值得做”上却不成立。流程能阻止粗心错误，却不能替人选择目标，也不能保证长 SPEC 没有遗漏。最值得保留的是：把隐性假设写成契约，用独立证据挑战实现，并由人对范围和结论负责。
